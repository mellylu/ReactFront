import { Outlet, Link } from "react-router-dom";

export default function Root() {
  return (
    <>
      <div>
        {/* other elements */}

        <nav>
          <ul>
            <li>
              ldnfkng</li>
          </ul>
        </nav>

        {/* other elements */}
      </div>
    </>
  );
}